# Step G の検査の図（2026-09-26）

決裁の指示（確認用の図は GitHub に入れる）。元は `outputs/g/`（Git 管理外、`scripts/42_g_checks.py` が描く）。

| ファイル | 中身 |
|---|---|
| `cond6_rtc_example.png` | 完了条件 6。左: 前の塊への重み（d=4、範囲 E=10・40 × 線形・指数）。右: 1 回の推論で、前の塊の残り（黒）・rtc（青）・同じ入力と同じ雑音の naive（赤の点線）の行動の 0 次元（正規化された空間）。灰色が最初の d 手 |

出所: `outputs/g/cond6.json`（R1 の 3 万手、rtc・s=10・d=4、種 198500〜198509、推論 102 回）。報告は掲示板 0066。
