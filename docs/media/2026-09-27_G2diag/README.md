# G2 の不合格の原因の切り分け（2026-09-27）

決裁 0068 の 1。元は `outputs/eval/G2diag/`・`outputs/results/G2diag/`（Git 管理外、`scripts/43_g2_diag.py`）。映像は左が俯瞰、右が手首のカメラ、20 fps。

| ファイル | 中身 |
|---|---|
| `handover_states.png` | 1 の (4)。制御が方策に戻った時点の状態（手と立方体の水平距離・指の開き・指先の高さ）を、復帰データ A・B・C の保存を始めた最初のこまと並べた。青がデータ、赤が評価の誘発、灰が診断 |
| `R1_P2hold_rtc_195300.mp4`・`_frames.png` | 診断（評価の条件ではない）: 落下中は手を止める P2（B と同じ形）、R1、**評価の設定（rtc・d=4・範囲 40）**。8.0 s に立方体の真上・指が開いた状態で制御が戻り、8.5 s には指を閉じて箱の方へ動く。立ち直らない |
| `R1_P2hold_sync_195300.mp4`・`_frames.png` | 同じ種・同じ診断を **sync** で。真上にとどまって下り、掴み直して 14 s までに箱へ入れる |
| `N1_P2hold_sync_195300.mp4` | 同じ種・同じ診断を N1（復帰データなし）で、sync。これも立ち直る |

報告は掲示板 0069。
