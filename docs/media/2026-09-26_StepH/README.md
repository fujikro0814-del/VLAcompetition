# Step H の学習の損失（2026-09-26）

決裁の指示（確認用の図は GitHub に入れる）。元は `outputs/train/<回>/loss.png`（Git 管理外、`recovla.policy.train_launcher` が描く）。細い線は 50 手ごとの記録、太い線はその移動平均（点の数の 1 割の窓。窓がそろうまでの頭の部分に段差が出るのは作図のため）。

| ファイル | 学習 | データ | 手数 | 損失（最初 → 最後、最後の 1 割の平均） |
|---|---|---|---|---|
| `R1_loss.png` | R1（復帰あり） | `R1cue_20260926-140506`（330 本・44,940 こま、手がかり 17 次元） | 30,000 | 1.84 → 0.020、0.0202 |
| `N1_smoke_loss.png` | N1 の 1000 手の試し | `N1cue_20260926-140506`（330 本・46,225 こま） | 1,000 | 0.754 → 0.089 |

出所: `outputs/h/train_R1.json`・`train_N1_smoke.json`。報告は掲示板 0060。
