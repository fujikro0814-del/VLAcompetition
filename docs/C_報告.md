# Step C 報告 — 新しい場所と移植の確認（判断点 G0）

作成: 2026-09-25 / 対象: `手順書_最終課題_生成復帰VLA_v2.md` §4 / 前提: `docs/B_提案書.md`（承認済み、掲示板 0005・0007）

## 要点

- **完了条件 1〜4 はすべて満たした。** 検査は `tests/test_c_port.py`（全 102 件の検査が合格。`.venv\Scripts\python.exe -m pytest`）
- **G0: 学習配置 20 回で 18 回成功**（予備実験と同じ種 100000〜100019）。成否は 20 試行すべてで予備実験と一致し、成功までの時刻もミリ秒の単位まで一致した。移植で動作は変わっていない
- **推論 1 回の時間は平均 0.27 秒**（同期実行、`select_action` の推論 71 回）。計画の d=2（0.2 秒）を超える。Step G の RTC の設定に関わるので、下の「判断が要ること」に挙げた
- 手違い 1 件: uv が Python を HKCU のレジストリに登録した。人の判断で学生用の値に戻し、手順を直した（掲示板 0008）

## 完了条件の結果

出所はすべて `outputs/g0/`（Git 管理外）の JSON。`scripts/02_g0_check.py`（1〜3）と `scripts/03_vla_listing.ps1`（4）が書き、`tests/test_c_port.py` が読んで判定する。

### 1. 取り込みと起動がネットワークなしで通る

`HF_HUB_OFFLINE=1`、`HF_HOME=<ROOT>\models\hf_home` で実行した（`offline_check.json`、`train10_check.json`）。

| 項目 | 結果 |
|---|---|
| (a) 出発点モデル smolvla_libero と前処理・後処理 | 読み込めた（7.4 秒、4.5 億パラメータ）。SmolVLM2 は方策の中から名前で呼ばれ、同じ HF_HOME から解決された |
| (b) 予備実験のチェックポイント（019000）と 1 回の推論 | 読み込めた。入力照合は 2 視点とも合格、正規化後の状態の最大絶対値 3.418（予備実験の記録と同じ値） |
| (c) 起動器の空打ちと 10 手の学習 | 空打ちは終了コード 0。10 手の学習も終了コード 0 で、保存点ができた（損失 3.34 → 0.87、映像用メモリの最大 7.69 GiB） |
| HF_HOME の下に C:\VLA を指すパスが残っていないか | 0 件 |

10 手の学習では、あわせて提案書 §10 の仕組みを確かめた（R8）。

- **統計量の固定**: 学習に渡すデータセットの写しの `meta/stats.json` を別の値に差し替えて学習すると、保存点の正規化・逆正規化の統計量（observation.state と action の mean・std）が差し替えた値とビット単位で一致した。`--policy.path` から始めても `meta/stats.json` から読まれるので、2 周目はこの方法で固定できる
- **学習率の上書き**: `--policy.optimizer_lr=3e-5`・立ち上がり 2 手・減衰 10 手・`decay_lr=2.5e-6` を渡した。ログの学習率は、LeRobot の予定（`CosineDecayWithWarmupSchedulerConfig`）を同じ設定で作り直した値と、10 手すべてで一致した（ログの 2 桁で比較。最後は 2.5e-6）
  - 最初はピークの 3e-5 がログに出ることを検査していたが、これは誤りだった。ログは各手の予定を進めた後の値なので、ピークは記録点の間に来る。検査を上の比較に直した

### 2. 複写した記録 1 本の再生確認（手先 5 mm）

`outputs/legacy/raw/ep_800000`（305 こま）で確かめた（`replay_check.json`）。

| 方式 | 手先の最大誤差 | 判定 |
|---|---|---|
| window（20 Hz の行動） | **0.32 mm** | 合格（許容 5 mm） |
| step（物理ステップごとの x_des） | 0.000 mm | 誤差はほぼ 0 だが、ビット一致ではない（画像の差は最大 1） |
| last_step（誤った定義、陰性対照） | 321 mm | 検出できた |
| shifted（誤った定義、陰性対照） | 11.2 mm | 検出できた |

step がビット一致しないのは、記録を作った環境（python311）と新しい環境が違うため。提案書 §3.1 で想定したとおりで、ビット一致は新しい環境の中だけで求める（流用元の検査 `test_step_replay_is_bit_exact` は、新しい環境で記録した 1 本で合格している）。

### 3. 予備実験のチェックポイントを新しい評価器で動かす（G0）

学習配置 0〜9 を 2 周、種 100000〜100019 で実行した（`eval_check.json`、評価の記録は `outputs/g0/eval_20260925-174348/`）。

- **成功 18/20**（基準は 15 以上）。予備実験も 18/20
- **20 試行すべてで、成否が予備実験と一致した。** 成功した 18 試行は、成功までの時刻もミリ秒の単位まで一致した。失敗は予備実験と同じ種 100000（配置 0、向き −41.3°）と種 100013（配置 3、+42.9°）
- 種と配置は 20 試行すべてで予備実験と同じ（配置の x・y・向きが完全に一致）
- **入力照合は 20 試行すべてで合格**（試行ごとに最初の推論で照合するように変えた。流用元は 1 回の実行で最初の 1 回だけ照合していた）
- **推論 1 回の時間**（`select_action` の前後、`torch.cuda.synchronize` 込み）:

| | 回数 | 時間 |
|---|---|---|
| そのプロセスで最初の推論 | 1 | 0.45 秒 |
| それ以外 | 70 | 平均 **0.273 秒**、中央値 0.273 秒、最大 0.380 秒 |

条件は RTC なし・同期実行で、推論はチャンクごと（50 手ごと）に 1 回。

### 4. C:\VLA の全ファイル一覧が Step A の開始時と一致する

`vla_listing_check.json` の結果:

- 開始時（Step A）148,972 件、今（17:42）148,972 件。差（パス・大きさ・更新日時）は **0 件**
- 一覧は C:\VLA のファイル名を含むので、Git 管理外の `docs/local/` に置いた

## 移植でしたこと

コミットは 3 つ（リモートの `Initial commit` の上）。

1. **069bfce**: Step B までの文書、掲示板、環境の定義、push 前の検査
2. **d71e9b5**: 流用元（コミット dbb2c3b）を `git archive` で取り出し、そのまま置いた。1 ファイル 1 行の複写記録は `docs/provenance.csv`（コード 95 行、モデル・記録 668 行。すべて複写の前後で SHA-256 が一致）。そのまま置いていないのは次の 3 つ:
   - `scene_g0.xml`: include の相対パスと `compiler meshdir` の 1 行を変えた（MuJoCo はメッシュの場所を主ファイルから数えるため）。コンパイル後のモデルは流用元とバイト単位で同じで、`tests/test_c_port.py` で検査している
   - `train_launcher.py`: docstring にあった学内の共有フォルダのパスを伏せた（公開リポジトリのため）
   - `stats.py`: `gui_core.py` から `wilson_interval()` だけを切り出した
3. **a2d764c**: 設定ファイル経由にし、台帳との結合を外した（動作は変えない）
   - 数値は `configs/default.yaml`（提案書 §5）と `configs/g0.yaml`（旧場面・旧評価）から読む。制御器の調整値は、予備実験の raw の meta.json に流用元が書いた値と一致する（検査あり）
   - `app.py`・`collect.py`・`scripted_demo.py` から必要な部品だけを `sim/control.py`・`sim/render.py`・`sim/device.py` に移した
   - 外したもの: 台帳、収録の状態機械、操作者の画面、`DEFAULT_RAW_DIR`・`scan_raw`・`SessionLog`、ラベルを要求するマニフェスト、環境変数による制御器・装置の切り替え
   - 流用元の検査（`tests/legacy/`、83 件）はすべて合格。外した機能の検査 8 件は持ち込んでいない（ラベルつきマニフェスト 7 件、持ち込まない設定例 1 件）。`test_replay` の fixture は、台帳なしで同じ入口と記録器から 1 本を記録する形に書き直した
   - 評価器に足した記録（行動は変えない）: 推論 1 回の時間、試行ごとの入力照合

## 手順書・提案書との食い違い、気づいたこと

- **固定一覧の件数**: 提案書の「全 95 パッケージ」は誤りで、正しくは 87 件（学生用の環境の dist-info と完全に一致）。これに追加の 18 件で、入った一覧は計 105 件
- **レジストリ**: `uv python install` は既定で Python を HKCU に登録し、学生用の Python を指していた項目を上書きした（掲示板 0008）
  - 人の判断で、学生用の値（InstallPath の 3 つ）に戻した
  - `env/setup_env.ps1` は `--no-bin --no-registry` に直した
- **流用元の raw の step 再生はビット一致しない**: 環境の違いによるもので想定内（上の 2）
- **最初の検査の実行が止まった**: tests/legacy の最初の一括実行が、CPU をほとんど使わないまま 12 分以上止まった。止めて実行し直すと、全件が 14 秒で終わった。原因は突き止めていない
- **掲示板の日時の誤り**: 0004〜0008 に、実際より後の時刻を書いていた。push の前だったので、ファイルの書き込み時刻に合わせて直接直した
- **GitHub の認証**: 人が gh で認証した（fujikro0814-del、トークンは Windows の資格情報の保管場所）。git からの push は、このリポジトリの設定で gh を資格情報の提供元にしている

## 判断が要ること

1. **推論時間と d（Step G）**
   - 今回の実測は平均 0.273 秒・最大 0.38 秒で、同期実行・RTC なしの値。RTC ありは誘導の勾配の分だけ長くなる見込み（提案書 R7）
   - 手順書の Step G の 1 は「実測が d×0.1 秒（d=2 で 0.2 秒）を超えたら報告する」としている。すでに RTC なしで超えている
   - 選択肢:
     - (a) d=2 のまま比べる。模擬の中では物理を止めて計るので、成否には影響しない。実時間との食い違いは発表で断る
     - (b) d=3（0.3 秒）を主にする
     - (c) d を実測から決める。Step G の最初に RTC ありで測ってから選ぶ
   - おすすめは (c) です。後戻りのしやすさ: どれも設定の値 1 つ（`runtime.delay_steps`）なので、Step G の前なら費用はない
2. **Step C の承認**。承認が来るまで Step D に進まない
