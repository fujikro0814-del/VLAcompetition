# Step C 完了と G0

- 番号: 0009 / 差出: main / 宛先: human、review / 日時: 2026-09-25 17:55（日本時間）
- 返信先: 0005
- 状態: 未読
- 関係する枝・PR・コミット: main（069bfce、d71e9b5、a2d764c と、この報告のコミット）

## 要点（3行以内）

Step C の完了条件 1〜4 はすべて満たした。G0 は学習配置 20 回で 18 回成功で、成否と成功の時刻は予備実験と全試行で一致した。
推論 1 回は平均 0.27 秒で、計画の d=2（0.2 秒）を超える（Step G の設定に関わる）。
Step C の承認と、d の扱いの判断をお願いしたい。承認まで Step D に進まない。

## 本文

詳しくは `docs/C_報告.md`。数字の出所は `outputs/g0/*.json`（Git 管理外）で、`tests/test_c_port.py` が判定する（全 102 件の検査が合格）。

| 完了条件 | 結果 |
|---|---|
| 1. ネットワークなしで取り込みと起動 | 出発点モデル・前処理・後処理、チェックポイントと 1 回の推論、起動器の空打ちと 10 手の学習、すべて通った。HF_HOME の下に C:\VLA を指すパスは 0 件 |
| 2. raw 1 本の再生確認 | window の手先の最大誤差 0.32 mm（許容 5 mm）。陰性対照 2 つも検出できた |
| 3. G0（種 100000〜100019） | **18/20**（基準 15）。20 試行すべてで成否が予備実験と一致し、成功の時刻もミリ秒の単位まで一致。入力照合は全試行で合格 |
| 4. C:\VLA の全ファイル一覧 | 148,972 件で、Step A の開始時との差は 0 件 |

- 10 手の学習で、提案書 §10 の仕組みも確かめた（R8）
  - 差し替えた `meta/stats.json` が、保存点の統計量にビット単位でそのまま入った
  - 学習率の上書きは、LeRobot の予定の計算と 10 手すべてで一致した
- 推論 1 回の時間（同期実行、RTC なし、`torch.cuda.synchronize` 込み）: 平均 0.273 秒、中央値 0.273 秒、最大 0.380 秒（70 回）。プロセスで最初の 1 回は 0.45 秒

## 求めること

1. Step C の承認
2. 推論時間と d の扱い（Step G）。RTC なしでも、すでに d=2（0.2 秒）を超えている
   - (a) d=2 のまま比べる。成否には影響しないが、実時間との食い違いを発表で断る
   - (b) d=3 を主にする
   - (c) Step G の最初に RTC ありで測ってから決める（推奨）
   - どれも設定の値 1 つなので、Step G の前なら後戻りの費用はない
