# Step E 完了: K1 は不合格。分岐と安全フィルタの判断を求める
- 番号: 0037 / 差出: main / 宛先: human、review / 日時: 2026-09-26 09:40（日本時間）
- 返信先: なし
- 状態: 未読
- 関係する枝・PR・コミット: main（このファイルと `docs/E_報告.md` を含むコミット）

## 要点（3行以内）

K1 は不合格（E6 の正答率 31.3%、閉ループの持ち上げ 3/30・成功 2/30、接触率 46.7%）。表の上では CFG の行（指示依存度 78.8%、中間落ち率 78.4%）。
分析では、方策は指示をほとんど見分けず、3 つの立方体の真ん中へ向かう（指示による分かれは必要な幅の 38%）。学習の手順の誤りではない。
分岐（CFG／LoRA／CFG の後に LoRA）と、安全フィルタを今作るかの判断を求める。詳細は `docs/E_報告.md`。

## 本文

`docs/E_報告.md` の「結果」「分析」「判定」を見てほしい。数字の出所はすべて `outputs/k1/*.json`（Git 管理外）。

- 使った種: E6 は 190000〜190032、閉ループは 191000〜191029（どちらも選択用の帯）
- 事故の報告（0032 の 4）と、そのやり直しの結果も同じ文書に書いた

## 求めること

1. 分岐
   - (a) 表どおり CFG（指示を空にした例を約 1 割混ぜて 8000 手をやり直し、推論で重み 1.5・2・3。CFG は本線が自作）
   - (b) LoRA（VLM の上位層。対象の層と rank は承認の後に提案）
   - (c) (a) を先に試し、だめなら (b)
   - 推奨は (c)
2. 安全フィルタ（接触率 46.7% ≥ 5%）: (i) 今から作る / (ii) 分岐のやり直しの後の接触率で決める。推奨は (ii)

答えが来るまで、分岐の実装と学習は始めない。0036（B の値）の答えが来れば、Step F の完了条件の試験は並行して進める（GPU の描画を使うので、学習とは同時に回さない）。
