# 0016〜0018 への回答

- 番号: 0019 / 差出: review / 宛先: cloud-runner、main / 日時: 2026-09-25 18:17（日本時間。チャットでの回答を cloud-runner が書き写した）
- 返信先: 0016、0017、0018
- 状態: 未読
- 関係する枝・PR・コミット: 枝 `cloud/runner`、https://github.com/fujikro0814-del/VLAcompetition/pull/3

## 要点（3行以内）

0016: 型の案を採る（実装は本線）。torch の種の変換は案のまま。台本は spawn_key (3, color_index, retry)、注入は (4, color_index, retry)。
0017: (b) を必須とし、(a) を 1 回だけ試す（403 なら書いて止まる。迂回しない）。
0018: A を採る（deliver には predict_action_chunk の出力のまま (H, 7)、後処理の前）。runner.md の修正は本線。

## 本文

（監督の回答をそのまま書き写す）

0016：型の案（common/seeds.py の STREAM_ID・seed_sequence・stream・streams・torch_seed）を採る。実装は本線。
  SeedSequence → torch の種は generate_state(1, dtype=uint64)[0] でよい。
  台本と注入の列は SeedSequence(layout_seed, spawn_key=(3, color_index, retry)) と (4, color_index, retry)。
  本線が common/seeds.py を入れた後に、§3 の検査を足す（別の PR でよい）。
0017：(b) を必須とする（本線が取り込む前に Windows で tests/policy を全件回す）。
  あわせて (a) を 1 回だけ試す：torch の CPU 版の取得をもう一度行い、通れば 60 件を回して結果を報告に足す。
  403 のままなら、そのことを書いて止まる。迂回はしない。
0018：A を採る。deliver に渡す塊は predict_action_chunk の出力のまま (H, 7)、後処理の前。
  runner.md の修正は本線が行う。この枝の検査は A=32 のままでよい（Schedule は A を問わない）。

PR の見張りは不要。上を済ませたら push して止まる。

## 求めること

cloud-runner: 0017 の (a) を 1 回試して結果を書き、push して止まる。
main: common/seeds.py の実装（0016）、runner.md の修正（0018）、取り込む前に Windows で `tests/policy` を全件回す（0017 の (b)）。
