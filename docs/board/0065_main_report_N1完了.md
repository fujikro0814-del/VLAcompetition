# N1 の 3 万手が完了（Step H の本番学習は R1・N1 ともそろった）
- 番号: 0065 / 差出: main / 宛先: human、review / 日時: 2026-09-26 22:55（日本時間）
- 返信先: 0060、0061、0063
- 状態: 未読
- 関係する枝・PR・コミット: main（学習は 6f6d2bc のコード。このファイルと `docs/media/2026-09-26_StepH/N1_loss.png` を含むコミット）

## 要点（3行以内）

N1 の 3 万手が 22:49 に完了（12,586 s、終了コード 0、GPU の最大 7.70 GiB、保存点 5000〜30000 の 6 つ）。損失は 1.69 → 0.019 で、最後の 2 割はほぼ平ら（R1 は 0.020）。
読み込んだのは 330 本・46,225 こま。R1（44,940 こま）とのこま数の差は 1,285 こま＝2.8%。理由は下の表のとおりで、Step F の完了条件 5 の値と同じ。
R1 と N1 で学習に効くコード・設定は同じことを確かめた。これから 0063 の段取りで Step G の残りの検査に入る。

## 本文

### N1（`outputs/h/train_N1.json`、`outputs/train/train_N1_20260926-191928_20260926-191928`）

| 項目 | 値 |
|---|---|
| データ | `outputs/datasets/N1cue_20260926-140506`（手がかり 17 次元、旗は記録のみ） |
| 読み込んだ本数・こま数 | 330 本・46,225 こま（データセットの指紋 87a01217…） |
| 手数・保存 | 30,000 手、5000 手ごと（005000・010000・015000・020000・025000・030000） |
| 時間・速さ | 12,586 s（3.50 時間）、平均 2.38 手/秒 |
| 損失 | 最初 1.692 → 最後 0.019（最小 0.018）。最後の 1 割の平均 0.0196、その前の 1 割 0.0197 |
| GPU の最大 | 7.70 GiB（上限 14 GiB を超えない） |
| 設定 | 種 1000、バッチ 32、行動エキスパートのみ、学習率は既定（LoRA・重み・手がかりのずらしはなし） |
| 外部への送信 | `--policy.push_to_hub=false`、`--wandb.enable=false`（`train_run.json` の command で確認） |
| コード | 6f6d2bc（未コミットの変更なし） |

損失の図: `docs/media/2026-09-26_StepH/N1_loss.png`。

### R1 と N1 の並び（0057・0061 の所見: 本数・こま数と、その差の理由をそのまま書く）

| | R1 | N1 |
|---|---|---|
| データセット | `R1cue_20260926-140506` | `N1cue_20260926-140506` |
| 読み込んだ本数 | 330 | 330 |
| 読み込んだこま数 | 44,940 | 46,225 |
| こま数の差 | — | +1,285 こま（+2.8%）。**理由**: 330 本のうち 240 本の通常デモは両者で同じエピソード。残りの 90 本が、R1 では復帰デモ（A・B・C、注入を確かめた後の 10 Hz の区切りから記録）、N1 では同じ配置・同じ目標の通常デモ（最初から記録）。復帰デモは注入の前の部分（配置から最初の掴みまで）を記録に含まないので、復帰の動きを足しても 1 本あたり短い。差 1,285 こま ÷ 90 本 ＝ 1 本あたり平均 14.3 こま（1.43 s）短い（0043 の見積もりは通常 13.96 s・復帰 12.06 s で 1.9 s）。Step F の完了条件 5 で測った 2.8% と同じ値（見積もり 3.7%、報告の境 10%） |
| 時間 | 12,724 s | 12,586 s |
| 損失（最初 → 最後、最後の 1 割の平均） | 1.84 → 0.020、0.0202 | 1.69 → 0.019、0.0196 |
| GPU の最大 | 7.69 GiB | 7.70 GiB |
| 学習のコード | c7886d9 | 6f6d2bc |

- コードの版は違うが、その間の変更は評価だけ（`configs/default.yaml` の `eval.P2.min_dist_from_box_m` 0.15 → 0.19、`runner.py` の手がかりの記録）。学習の起動の部分（`train_launcher` の指紋 b3859d15…）、`recovla.policy.train_wrapped`・`recovla.data` と学習の設定は同じ
- 損失は R1・N1 とも同じ形で下がった。保存点は損失ではなく、選択用の種での成功率で選ぶ（0061・0063）

### この後（0055・0062・0063 の順番）

学習・生成は止まっていることを確かめた（学習の子のプロセスなし、GPU 0.9 GiB）。
1. Step G の残りの検査（決まりに依らない）: 完了条件 1（191000〜191019）・2（198300〜）・4 を方策で（198400〜、P1〜P3 各 20 回）・6（198500〜）
2. 遅れ d の測定（R1 の 3 万手、198600〜、100 回以上）→ `runtime.delay_steps`
3. RTC の設定の 2×2（192000〜192029、P2 各 30 回、分母は 30 回・成立率も記録）
4. 保存点の選択（R1・N1、2 万手と 3 万手、193000〜193029、全候補で同じ実行の設定）
5. G2（194000〜・195000〜195049。0064 の返事を待つ）

種は使うたびに `docs/種の台帳.md` を更新する。

## 求めること

なし（報告）。
