# 0071 への判断（刻み数は閉ループで確かめてから、検出器の閾値を承認、R2 の生成はその後）
- 番号: 0072 / 差出: human（決裁）/ 宛先: main / 日時: 2026-09-27 02:25（日本時間。本線が書き写した時刻）
- 返信先: 0071
- 状態: 既読（本線）
- 関係する枝・PR・コミット: main（このファイルを含むコミット）

## 要点（3行以内）

A の復帰データは遅れのない実行ではっきり効き、評価の設定では効きが消える、を中心の結果として書く。d=4 は変えない。triton は入れない。
刻み 10・5・3 を閉ループで確かめ、結果の前に固めた決まり (a)〜(d) で採否を決める（d は候補ごとに閉ループで測り直す）。R2 の生成はその後。
検出器の閾値（掴み損ね 3.0 s・その他の落下 1.0 s）を承認、当てはめをやり直す。

## 本文（決裁の回答の原文）

決裁の回答（0071）

■ 受け取ったこと
- 2 の (1) を了承する。遅れのない実行では A の復帰データがはっきり効き（24 対で R1 15・N1 2、p = 0.001）、評価の設定ではその効きが消える。これはこの課題の中心の結果なので、変更一覧 v3 の「結果として書くこと」に加える。C の上積みは、この数でははっきりしないと書く
- 今の推論（刻み 10）を固定した入力で測ると p95 が 0.41〜0.42 s で、決まりなら d=5 になることを了承する。ただし d=4 は決まりどおり閉ループの測定で決めたものなので変えない。測り方によって境目をまたぐことは報告事項として残す
- inductor のコンパイルのための triton の追加は行わない

1. 刻み数を減らす手は、閉ループで確かめてから採否を決める。R2 の生成はその後とする
  - 候補: 刻み 10（今の設定、d=4）、刻み 5、刻み 3。刻み 8 は d が下がらないので外す
  - 各候補の d は、元の d を決めたときと同じ方法（閉ループ、ウォームアップを除いて 100 回以上、p95 / 0.1 s の切り上げ）で測り直す。固定した入力での見積もり（d=3・d=2）と違った場合は、閉ループの値を使う
  - RTC の設定（範囲 40・指数）と s=10 は変えない
  - 試行（R1 の 3 万手、候補ごと）: 自然 30 回（選択用の帯の未使用の新しい種）、P1（195100〜195129）・P3（195100〜195129）・P2（195000〜195029）の誘発を各 30 回。誘発の種は、これまでの診断と同じ種にして対にする
  - 決め方（結果の前に固定する）:
    (a) 自然の成功数が、刻み 10 から 3 回以内の候補だけを残す
    (b) 残った候補のうち、P1・P2・P3 の復帰の合計（成立した試行が分母）が最も多い候補を選ぶ
    (c) (b) で選んだ候補の復帰の合計が、刻み 10 より 4 以上多くなければ、刻み 10 のままとする（差のない変更はしない）
    (d) 同じ数なら刻みの多い方（今の設定に近い方）とする
  - あわせて、グリッパの開閉が失敗の原因になった試行（開くべきでない所で開いた、など）を候補ごとに数えて示す
  - 採った場合: 以後の評価（R2 の生成、R2・R1+・R1・N1 の評価、E の評価）はすべてその刻み数と d で行う。G2 までの R1・N1 の数字は刻み 10・d=4 のものとして残し、変更の理由と日付を変更一覧 v3 に書く
  - 種の割り当てと所要時間の見込みを掲示板に一行で出してから回してよい（決まりは上で承認済み）

2. 失敗の検出の閾値: 案のとおり承認する（掴み損ねの窓 3.0 s、その他の落下の静止 1.0 s）
  - 新しい閾値で、G2 の成功した試行（R1 96・N1 96）への当てはめをやり直し、誤発火の件数を示す
  - P1 の誘発の成立の条件（2 s）は変えない。誘発の定義は固定済みのため。本当は掴めていたのに成立と数えた P1 の本数は、条件ごとに報告に書く

3. R2 の実装と小さな試しの結果を了承する

4. R2 の生成（本番の種 40000〜）は、1 の結果の報告と、その回答の後に入る

次の報告: 1 の結果と採否（決まりに当てはめた結果）、2 の当てはめのやり直しの結果。そこで止まる。

## 本線の対応

- 種の割り当てと所要時間の見込みは 0073（一行の知らせ）に書いてから回す
- 変更一覧 v3 の「結果として書くこと」に 2 の (1) を足した。triton は入れない
