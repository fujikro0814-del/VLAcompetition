# R2 の生成の検査と目視用の資料（承認を求める）、接触率が基準に当たった（安全フィルタの前に諮る）、成立率の内訳、RTC の書き方
- 番号: 0078 / 差出: main / 宛先: human、review / 日時: 2026-09-27 03:41（日本時間）
- 返信先: 0077、（0077 への回答＝下の「回答の原文」）
- 状態: 未読
- 関係する枝・PR・コミット: main（生成は 52d1717 から、このファイルを含むコミット）

## 要点（3行以内）

R2 は、本線の naive d=4 で 4 種類とも割り当ての本数に達した（P1・P2・P3 各 40、甲 60）。検査 (a)・(b) も合格。目視用の資料は `docs/media/2026-09-27_R2/`。**目視の承認を求める（承認まで学習しない）**。
**接触率が基準に当たった**: naive の自然で 3/30（10%）。手順書の 5% 以上に当たるので、安全フィルタを作る前に諮る。3 本とも成功した試行で、箱に置く間際に箱の壁へ短く（0.05〜0.35 s）触れたもの。案は「99 回で測り直してから決める」。
成立率が下がったのは種の配置による（P1 で 3 候補とも成立しない種が 8。前の種の帯では 0）。RTC の書き方は下の 3 に原文を示す。

## 回答の原文（0077 への回答、チャットで受け取ったもの。書き写し）

回答（0077）

■ 了承すること
- commit の順番、start.json に始めた時点の版を書くようにしたこと、3 本とも 52d1717・未 commit の変更なし・naive d=4 で始まったことを確認した
- 決め直しの結果、本線を naive d=4（RTC なし）とすることを確認した。決まりに当てはめた結果のとおりで、以後は R2 の結果がどうであっても変えない
- G2 の判定は元の設定の記録として残し、naive の P2 の立ち直り 4/20 を参考として並べることを了承する
- 3 の訂正（閉じた時点のずれは 1.9〜3.3 cm、手がかりは正しかった）を了承する。自然な失敗の主な形は「手がかりは正しいのに、ずれた位置で閉じる」で、評価の P1 とほぼ同じ形であることを、変更一覧 v3 の「結果として書くこと」に加える

1. 0078 に次を加える
  (1) 接触率: 決め直しの 3 候補それぞれの接触率（自然と誘発を分けて）。naive は継ぎ目の跳びが大きいので、手順書の基準（5% 以上なら安全フィルタを作る）に当たらないかを確かめる。当たった場合は、作る前に諮る
  (2) 誘発の成立率: P1・P2 が 19〜21/30 に下がった理由の内訳（隙間不足、発動せず、閉じても持ち上がった、など）を候補ごとに示す。これまでの種（P1 25/30、P2 40/50）との違いが、種の帯の配置の違いによるものかも見る。定義は変えない
  (3) 変更一覧 v3 で、本線が RTC なしになったことの書き方を原文のまま示す。計画書で RTC を技術の柱の一つとしていたこととの関係（本線では使わず、E4 で滑らかさと立ち直りの取引を示す比較の条件とする）が分かる書き方にする

2. R2 の目視用の資料では、次を最初に見られるようにする
  - 引き継ぎの時点の状態の分布を、決め直しの naive の評価での状態（制御が戻った時点）と重ねた図
  - 種類ごと 5 本の映像のうち、少なくとも 1 本は前半の方策の走りで継ぎ目の跳びが目立つ場面を含むもの

次の報告: 0078（R2 の生成の検査、目視用の資料、上の 1 の (1)〜(3)）。そこで止まる。

## 本文

### 1. R2 の生成と検査（`outputs/gen/R2_<種類>_20260927-*`）

- 始めた時点は 3 本とも 52d1717、未 commit の変更なし、runtime は naive・d=4（`start.json`）。甲も同じ（03:26 開始）
- 生成は 03:23〜03:30。同時 3 本で、エラーはなし

| 種類 | 試み | 引き継いだ | 保存 | (a) 台本の成功率 | 捨てた理由 | 引き継ぎのきっかけ | 使った種 | こま数 |
|---|---|---|---|---|---|---|---|---|
| P1 | 49 | 40 | 40（成立の時点 20・成立の後 20） | 100% | 閉じても持ち上がった 9 | 成立 40 | 40000〜40050 | 10,820 |
| P2 | 50 | 40 | 40（20・20） | 100% | 着地の隙間不足 9、引き継ぐ前に時間切れ 1 | 成立 40 | 41000〜41050 | 10,329 |
| P3 | 48 | 40 | 40（20・20） | 100% | 引き継ぐ前に成功 7、着地の隙間不足 1 | 成立 40 | 42000〜42048 | 10,094 |
| 甲 | 62 | 61 | 60 | 98.4% | 台本の置き外し 1、引き継ぐ前に成功 1 | **ランダムな時刻 58、机の上の落下の検出 2** | 43000〜43061 | 11,660 |

- (a) 4 種類とも 90% 以上で合格
- (b) 最初のこまが 10 Hz の境目にある（全区間）。保存した開始状態からの再生（種類ごとに 10 本、step と window の 2 通り）も全部合格

### 2. 目視用の資料（`docs/media/2026-09-27_R2/README.md`）

**最初に見る図: `handover_vs_eval_naive.png`**。R2 の引き継ぎの時点（赤）と、決め直しの naive の評価で制御が戻った時点（緑、成立した試行）を重ねた。評価の状態のうち、同じ種類の R2 の最小〜最大の外にある数:

| | 評価の本数 | 外（どれかの量） | 外れた量 | 閉じている（評価／R2） |
|---|---|---|---|---|
| P1 | 21 | 6 | 指先の高さ（評価 2.0〜2.8 cm、R2 の最小 2.3 cm） | 21/21 ／ 11/40 |
| P2 | 20 | 6 | 指先の高さ（評価 14.8〜16.7 cm、R2 の最大 15.6 cm。外れは 1.1 cm 以内） | 0/20 ／ 4/40 |
| P3 | 27 | 1 | 指先の高さ | 0/27 ／ 6/40 |

**最初に見る映像: 名前の末尾が `_seam` の 4 本**（種類ごとに 1 本）。前半の方策の走りで、継ぎ目の跳びがいちばん大きい場面を含む。跳びの前後 0.5 s は、映像に `<<SEAM JUMP` と出る。

| 種類 | 映像 | 跳び | 時刻 | 回し直した 10 本の、試行ごとの最大の跳びの中央値 |
|---|---|---|---|---|
| P1 | `R2P1_40011_delayed_seam` | 0.15 m/s | 6.4 s | 0.077 m/s |
| P2 | `R2P2_41003_delayed_seam` | 0.20 m/s | 6.4 s | 0.083 m/s |
| P3 | `R2P3_42007_delayed_seam` | 0.20 m/s | 14.4 s | 0.115 m/s |
| 甲 | `R2natural_43007_detect_or_random_seam` | 0.14 m/s | 1.4 s | 0.009 m/s |

- **継ぎ目の映像は回し直したもの（保存しない）**。生成のときは前半の行動を記録していなかったので、保存した区間の先頭 10 本を、同じ種・同じ設定で回し直した。方策が出した行動と塊の切り替わりを記録し、誘発が行動を上書きしたこまを除いて跳びを測った
  - 回し直しに使った種は、生成と同じ R2 の帯の種で、データは作っていない
  - 目視用の資料の作り方の中で決めたので、掲示板で先に知らせる手順は取っていない
- GPU の描画の揺れがあるので、引き継ぎの時刻と状態が保存した記録と一致したもの（P1 10/10・P2 9/10・P3 7/10・甲 10/10）の中から選んだ
  - 見出しを直すためにもう一度回し直したところ、P3 は 1 回目に選んだ 42001 が一致しなくなり、42007 に変わった（`seam_pick_<種類>.json`）
- 評価（naive）の継ぎ目の跳びの中央値は 0.045 m/s。映像の跳びはその 3〜4 倍の場面だが、1 手（0.1 s）あたり 2 cm の差なので、映像では小さな「かくっ」とした動きに見える程度
- **ほかの映像**は種類ごとに 4 本で、生成中にそのまま書いたもの。前半は方策の走り（記録しない）、後半は保存した区間（台本）

**図から読めること**（判断は求めない。報告）
- **P3**: 評価の状態は R2 の範囲にほぼ入る
- **P2**: 手の距離は R2 の範囲に入る（R2 は 6.8〜28.2 cm と広い）。指先の高さは、評価の上の端（15.6〜16.7 cm）が 1 cm ほど外れる
- **P1**: 評価で制御が戻るのは閉じた直後で、低い姿勢（指先 2〜3 cm、閉）にある。R2 の引き継ぎは成立の時点（閉じてから 2 s の確かめの後）なので、多くは方策がもう開いて上がっている。**閉じた直後の低い姿勢は、R2 では 40 本中の数本でしか覆えていない**。成立は 2 s たたないと決まらないので、乙の作り方では避けられない（0069 の 3 の設計どおり）
- **甲**: 失敗の検出で引き継いだのは 2 本（机の上の落下）だけで、58 本はランダムな時刻（1〜9 s）で引き継いだ。自然な失敗の主な形（ずれた位置で閉じる掴み損ね）は、検出が確定するのが閉じてから 3 s 後（6 s 台）なので、多くはその前のランダムな時刻に引き継いでいる。閉じている状態での引き継ぎは 41/60 ある

### 3. 回答の 1 の (1): 接触率（決め直しの 3 候補、`outputs/eval/RTCREDO`）

接触は、目標以外の立方体と箱の壁に、ロボットが触れた試行の割合（`metrics.contacts_n > 0`、G2 と同じ数え方）。

| | rtc 範囲 40 | rtc 範囲 10 | naive d=4（本線） |
|---|---|---|---|
| 自然 | 1/30（3.3%） | 0/30（0%） | **3/30（10%）** |
| 誘発（P1・P2・P3 の合計） | 24/90（27%） | 30/90（33%） | 26/90（29%） |
| うち P1・P2・P3 | 9・6・9 | 5・14・11 | 5・13・8 |
| 参考: G2（rtc 範囲 40、自然 99） | R1 3/99・N1 2/99 | — | — |

- **naive の自然が 3/30 = 10% で、手順書の基準（5% 以上なら安全フィルタを作る）に当たった**。95% 区間は 3.5〜25.6%（Wilson）
- 3 本（193205・193206・193222）は、どれも**成功した試行**。8.5〜10.7 s の、箱に置く間際に箱の壁（wall_yn）へ 1〜7 こま（0.05〜0.35 s）触れたもの。周りの立方体には触れていない
- 誘発の試行の接触は、誘発そのもの（ずらした立方体や、落とした立方体の近く）を含むので、基準の判定には使っていない（G2 と同じ扱い）。3 候補で大きな差はない

### 4. 回答の 1 の (2): 誘発の成立率の内訳

**候補ごとの、成立しなかった理由**（定義は 6f6d2bc のまま）

| | rtc 範囲 40 | rtc 範囲 10 | naive |
|---|---|---|---|
| P1 成立 | 19/30 | 20/30 | 21/30 |
| P1 閉じても持ち上がった | 11 | 10 | 9 |
| P2 成立 | 19/30 | 21/30 | 20/30 |
| P2 着地の隙間不足 | 9 | 9 | 8 |
| P2 発動しなかった | 2 | 0 | 2 |

**種の帯の配置によるか**（同じ種を、どの候補でも成立しなかったか）

| | 種の数 | どの候補でも成立しない種 | どの候補でも成立した種 |
|---|---|---|---|
| P1 195400〜（今回、3 候補） | 30 | **8** | 18 |
| P1 195100〜（前回。同じ 3 通り: rtc 範囲 40・範囲 10・naive） | 30 | **0** | 24 |
| P2 195400〜（今回、rtc 範囲 40 だけ） | 30 | 11 | 19 |
| P2 195000〜（G2、rtc 範囲 40） | 50 | 10 | 40 |

- **P1**: 成立しなかった試行は同じ 8 種に集まっている。今回の帯には、どの実行の設定でも「ずらして閉じても持ち上がる」配置が 8 あり、前回の帯には 0。差は偶然とは言いにくい（Fisher p = 0.005）。実行の設定ではなく配置で決まっている
  - 配置の種類・開始姿勢の割合は、2 つの帯でおおむね同じ。どの配置の性質（立方体の向きなど）が効いているかまでは調べていない
- **P2**: 隙間不足も同じ種で起きている。割合（37% 対 20%）の差は、配置の標本の揺れの範囲（Fisher p = 0.12）
- 以上から、成立率の低下は、種の帯（配置の標本）によるものと読む。定義は変えていない。最終評価（E2、各 50）でも成立率を併記する

### 5. 回答の 1 の (3): 変更一覧 v3 の書き方（原文のまま）

表の 22（0077 で追加、そのまま）:

> | 22 | **本線の非同期の設定（RTC の有無と範囲）**（2026-09-27 に決め直した） | 19 のとおり rtc 範囲 40・指数 | **naive d=4（RTC なし）**。d=4・刻み 10・s=10 は変えない。以後の評価（R2 の生成、R2・R1+・R1・N1、E）はすべてこの設定。G2 までの R1・N1 の数字は rtc 範囲 40 のものとして残す（G2 の判定＝不合格も元の設定の記録のまま）。R2 の結果によらず変えない | 19 の決まりは成功数の絞り込みが 4 組とも P2 0/30 で効かず、滑らかさだけで決まっていた。P1 の立ち直りを止める主な原因が RTC の寄せと分かったので、立ち直りを含めた決まり（自然の成功数で絞る → 立ち直りの合計、3 以内は同じ → 継ぎ目の跳び → 今の設定に近い順、`scripts/48_rtc_redo.py`、結果の前に commit）で、新しい種（自然 193200〜・誘発 195400〜）で決め直した。**結果**: 自然 29・28・28/30、立ち直りの合計は rtc 範囲 40 が 5・範囲 10 が 13・naive が 19 → naive。代わりに、継ぎ目の跳びの中央値は約 4 倍（0.011 → 0.045 m/s） | 0075・0076・0077 |

表の 23（今回追加、計画書との関係）:

> | 23 | **計画書での RTC の位置づけ**（計画書 v2 の §1 の概要「実行は RTC で 1 秒ごとに作り直す」、§4.3、§5 の E4、§9 の発表の「RTC あり／なし」） | RTC を技術の柱の一つとし、本線の実行を RTC（s=10）で行う | **本線（作品の実行、R2 の生成、E1〜E3・E5〜E8 の評価、最終のデモ）は RTC を使わず、1 秒ごとに作り直す非同期の naive（s=10・d=4）で行う。RTC は E4 で、同じ種で sync・naive・rtc を並べ、「滑らかさ（継ぎ目の跳び）と立ち直りの取引」を示す比較の条件として残す**。発表の「RTC あり／なし」の比較も、E4 の結果として示す。デモの「RTC で固定した部分と自由な部分の色分け」は、本線が RTC なしのため Step J で見直す | 遅れ d=4 のもとでは、前の塊に寄せる RTC が、失敗の直後の立ち直りを止めた（決め直しの P1: rtc 範囲 40 で 0/19、naive で 11/21）。一方、滑らかさは RTC の方がよい（継ぎ目の跳びの中央値 0.011 対 0.045 m/s）。計画書が RTC を入れた理由（作り直しの頻度を上げても継ぎ目で方針が飛ばないように）のうち、「継ぎ目が滑らかになる」ことは確かめられたが、立ち直りとは両立しなかった。これを E4 で示す | 0070 の 8・0075・0077・0078 |

「確定した扱い」の作品の方針（0070 の 7）は、「中身は同日 R2 の前に 0075 の 1 で naive d=4 に決め直した＝表の 22」と書き足してある（0077）。「結果として書くこと」の 7 に、自然な失敗の形（手がかりは正しいのに、ずれた位置で閉じる）を加えた。

### 6. 使った種（`docs/種の台帳.md` を更新）

R2 の本番: 40000〜40050・41000〜41050・42000〜42048・43000〜43061。継ぎ目の映像の回し直しは、このうちの保存した区間の先頭 10 本ずつ（データは作らない）。

## 求めること

1. **R2 の目視の承認**（`docs/media/2026-09-27_R2/`）。承認まで変換・学習をしない
2. **接触率が基準に当たった件（安全フィルタ）**: 次のどれにするか
   - **(a) 推奨: 作る前に、naive の自然を 99 回（G2 と同じ数、選択用の帯の新しい種）で測り直す**。5% 以上なら安全フィルタを作り、5% 未満なら作らない。30 回では区間が 3.5〜25.6% と広く、G2 の判定は 99 回で行っていたため。GPU で約 10 分。R2 の目視の承認の待ちと並行できる
   - (b) 基準どおり、いま安全フィルタを作る（B 提案書の IK の手前のもの）。作ったら、本線の設定は naive＋安全フィルタになる。R2 の前半（方策の走り、記録しない）はフィルタなしで走らせたもの。保存した区間は台本なので、データには直接は響かない
   - (c) 作らない。接触はどれも成功した試行での箱の壁への短い接触で、周りの立方体には触れていないため。ただし、手順書の基準の数え方（壁を含む）を変えることになる
3. 上の 2 で安全フィルタを作る場合、R2 を作り直すか（本線の案: **作り直さない**。理由は (b) に書いたとおり）
