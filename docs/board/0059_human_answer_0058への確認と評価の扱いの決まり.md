# 0058 への確認と、評価での扱いの決まり（決裁の回答を本線が書き写した）
- 番号: 0059 / 差出: human（本線が書き写し） / 宛先: main、review / 日時: 2026-09-26 18:31（日本時間）
- 返信先: 0056、0057、0058
- 状態: 対応中
- 関係する枝・PR・コミット: main

## 要点（3行以内）

0056 の条件 1〜3 はすべて満たした。P2 を使う評価（RTC の設定の 2×2、E2、E4、G2）に 0.19 m で入ってよい（順番は 0055 のとおり学習の後）。誘発の定義はこれで固定し、以後は変えない。
評価での扱いの決まり: 不成立の試行を別の種に差し替えない。復帰率の分母は成立した試行。R1 と N1 は両方で成立した種の対の比較も出す。
次の報告は R1 の完了と N1 の開始の時点。その間は 0055 の範囲で Step G を続けてよい。

## 本文

チャット（2026-09-26）での決裁の回答:

回答（0056 の条件の確認）: 下見のやり直しを確認した。条件 1〜3 はすべて満たしている。P2 を使う評価（RTC の設定の 2×2、E2、E4、G2）に 0.19 m で入ってよい。ただしどれも学習の後に回すものなので、順番は 0055 の決裁のとおり。

所見:
- P1 18/20・P2 18/20・P3 20/20 で、どれも 16/20 以上。誘発の定義はこれで固定し、以後は変えない（評価の途中で誘発を直すと、比べる数の意味が変わるため）
- P2 の「他の立方体の上に乗った」の分類の新設は了承。これまで「静止しない」にしていた件の分類の付け替えも、理由を添えて記録に残す
- P2 で着地点が他の立方体に近すぎる件は、成立の条件で弾けているので、着地点の選び方は今は変えない
- P1 の 2 件（指が縁を挟んで付いてきた、どちらも先客のある配置）も不成立として数えるだけでよい。先客のある配置で P1 が成立しにくい傾向は、E2 の報告で配置の種類ごとの成立率として示す

評価での扱いの決まり（先に固定しておく）:
- 不成立の試行は、別の種に差し替えない。種の組は全条件で同じにし、成立率はそのまま報告する
- 復帰率の分母は、その誘発が成立した試行の数とする。R1 と N1 を比べるときは、両方で成立した種だけの対の比較も併せて出す

その他:
- 手がかりの記録（cue_at_inference）を了承。方策の入力を変えていないことを確認した
- v3 の変更一覧（docs/変更一覧_v3.md）は受け取った。こちらで読み、指摘があれば別に返す
- Step H: R1 の 3 万手の完了（19:20 ごろの見込み）を待つ。次の報告は R1 の完了と N1 の開始の時点でよい。その間は、学習と取り合わない Step G の作業を 0055 の決裁の範囲で続けてよい

## 本線の対応

- **誘発の定義の固定**: `src/recovla/eval/induce.py` と `configs/default.yaml` の `eval.P1〜P3` を、このコミットの版で固定する（以後、評価の途中では変えない）。固定した版のコミットは、このファイルを含むコミット
- **分類の付け替えの記録**: 0.15 m の下見（`outputs/results/induce_script_P2_015.json`）の P2・種 198119 の理由 `no_rest` は、0058 で原因を調べた結果、落とした緑の立方体が青の立方体の真上に乗って静止していた（高さ 5.9 cm、傾き 1.5°）ので、`landed_on_cube` に付け替える。付け替えの理由: 以前の分類は「成立の判定の時間内に机の上で静止しなかった」をすべて `no_rest` にしており、他の立方体の上で静止した場合を区別していなかった（`recovla.eval.induce` の時間切れの分岐で、7672822 から区別する）。元の記録ファイルは書き換えず、この文書を付け替えの記録とする
- **評価での扱い**: `recovla.eval.report` は、復帰率の分母を成立した試行の数にしている（`summary.csv` の `recovery_rate`）。対の比較（`paired.csv`）の `recovered` は両方で成立した種だけ。不成立の試行の差し替えはしない（`41_results.py run` は種の並びをそのまま回す）
- **E2 の配置の種類ごとの成立率**: `report.py` の `tables.md` に「配置の種類ごとの誘発の成立率」の表を足した（このコミット）
