# 0062 への判断（学習の後の決まりを補足つきで承認）
- 番号: 0063 / 差出: human（決裁）/ 宛先: main / 日時: 2026-09-26 20:46（日本時間。本線が書き写した時刻）
- 返信先: 0062
- 状態: 既読（本線）
- 関係する枝・PR・コミット: main（このファイルを含むコミット）

## 要点（3行以内）

0062 の 1〜4 を補足つきで承認。d は「d=2」の全箇所を置き換え、RTC の 2×2 の成功数の分母は不成立も含めた 30 回、保存点の選択の実行の設定は全候補で固定。
種は G2 を終わりまで書き、d の種は使った範囲を記録し、種の台帳を作る。E3 の検定はまだ決めず、予備評価の前に対のマクネマー検定を主とする案を出す。

## 本文（決裁の回答の原文）

決裁の回答（0062）: 学習の後の決まりの案を、次の補足つきで承認する。

1. 遅れ d: 案のとおり承認
- 補足: 決まった d は、手順書・計画書で「d=2」と書いてある箇所（保存点の選択、G2、E4 など）をすべて置き換える。測った値が 2 でも 2 以外でも同じ扱いとする。変更一覧 v3 に載せる

2. RTC の設定の 2×2: 案のとおり承認
- 補足: 4 組の成功数は、P2 が不成立だった試行も含めた 30 回を分母とする（同じ種の組なので条件間で揃う）。成立率も併せて記録する

3. 保存点の選択: 案のとおり（2 万手と 3 万手、30 回ずつ、同数なら 3 万手）承認
- 補足: 実行の設定を明記する。rtc・s=10・上で決めた d・上で決めた RTC の設定とし、この設定を R1・N1 の全候補に固定して当てる（RTC の設定を 3 万手で決めたことによる偏りを、全候補で同じにするため）

4. 種の割り当て: 案のとおり承認
- 補足: G2 の種は終わりまで書く（自然 99 回は 194000〜194098、P2 誘発 50 回は 195000〜195049）。遅れ d の種も使った範囲を記録する
- 補足: 種の台帳（docs/種の台帳.md）を作り、帯・用途・使用済みの範囲を一か所にまとめる。以後、種を使うたびに更新する。Step I 以降と E1〜E8 の予備評価に使う帯は、まだ割り当てず空けておく

5. E3（R1 と N1 の比較）の検定の方法は、今は決めなくてよい。E3 の予備評価に入る前に、同じ種の対での比較（マクネマー検定）を主とする案を出す

見込みの扱い: 承認が N1 の完了より後になった場合の段取り（決まりに依らない Step G の残りの検査を先に回す）も了承する。

## 本線の対応（このコミットで）

| 補足 | 対応 |
|---|---|
| 1 | `docs/変更一覧_v3.md` の 18 に載せた。以後の評価はすべて測った d で回す |
| 2 | `41_results.py decide-rtc` を足した（0062 の手順 1〜4 をそのまま実装。分母は全 30 回、成立率も `outputs/results/rtc_decision.json` に残す）。変更一覧の 19 |
| 3 | `41_results.py decide-ckpt` を足した（成功数の多い方、同数なら 3 万手）。実行の設定は全候補で固定。変更一覧の 20 |
| 4 | `docs/種の台帳.md` を作った。`dcal` の既定を 0062 に合わせ（種 198600〜、100 回）、使った種の範囲を `outputs/results/dcal.json` に書くようにした。G2 の自然 99 回の種の数え方は 0064 で確認 |
| 5 | E3 の予備評価の前に案を出す（STATUS の予定に書いた） |
