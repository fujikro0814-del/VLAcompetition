# 公開に伴う食い違い

- 番号: 0003 / 差出: main / 宛先: human、review / 日時: 2026-09-25 16:55（日本時間）
- 返信先: 0002
- 状態: 未読
- 関係する枝・PR・コミット: なし（まだ何もコミット・push していない）

## 要点（3行以内）

0002 の指示に照らすと、いまの docs\ にはそのまま公開できないファイルが 3 つある。
掲示板の README にも「非公開のまま」という決まりが残っている。
この 5 点が決まるまで、最初のコミットと push はしない。

## 本文

<ROOT> の docs\ を検索した結果（`docs\*.md` への文字列検索と、一覧の CSV の先頭の確認）:

| # | 対象 | 問題 | 0002 の該当 |
|---|---|---|---|
| 1 | `docs\A_vla_listing_before.csv`、`docs\A_vla_listing_after.csv`（各 20 MB） | C:\VLA の全ファイルのパス・大きさ・日時の一覧。卒研の資料のファイル名と、99_台帳 以下のファイル名を含む | 2 |
| 2 | `docs\B_提案書.md` の冒頭（Step B で確かめた事実の 1 項目め） | 学内のプロキシのアドレスを書いている | 3 の趣旨（掲示板ではないが公開される） |
| 3 | `docs\A_調査報告.md` §2-11 と対応表 | 学内の共有フォルダ（Y:）のパスをそのまま書いている。個人を特定できるフォルダ名を含む | 3 の趣旨 |
| 4 | `docs\board\README.md` の決まり 4 | 「リポジトリは非公開のまま」と書いてある | 0002 と食い違う |
| 5 | `<ROOT>` の Git の作者のメールアドレス（提案書 §14 の 7） | 公開のリポジトリでは、コミットの作者のメールアドレスが誰にでも見える | 3 の趣旨 |

補足: リモートにはすでに `Initial commit`（README.md 1 行だけ）がある。<ROOT> の履歴はこのコミットの上に積む。提案書 §2.1 の「最初のコミットを流用元をそのまま取り込んだ状態にする」は、「取り込みのコミットを、リモートの最初のコミットの次に置く」と読み替える。差分が見えるという目的は変わらない。

## 求めること

1. 一覧の CSV: (a) Git 管理外の `docs\local\` に移し、`.gitignore` に足す（推奨）。(b) 消す。Step C 完了条件 4 の比較には、手元に残す (a) が要る
2. 提案書のアドレス: 公開する版では `<学内のプロキシ>` に置き換える（推奨）。元の記述は手元のメモ（Git 管理外）にだけ残す
3. 調査報告の共有フォルダのパス: 2 と同じく `<共有フォルダ>\99_台帳\…` に置き換える（推奨）
4. 掲示板の README の決まり 4: 「公開のリポジトリ。共同作業者は追加しない。鍵・個人の情報・学内の回線の設定・C:\VLA の記録とデータを置かない」に改める。最初のコミットの前なので、README を直接直すか、決まりどおり新しい番号で改めるか
5. 作者名とメールアドレス: GitHub の非公開用のアドレス（`<数字>+fujikro0814-del@users.noreply.github.com`。GitHub の Settings → Emails に表示される）を、このリポジトリの設定だけに使う（推奨）

後戻りのしやすさ: どれも push の前なら費用はない。一度 push すると、履歴から消すには書き換えと force push が要る。すでに複製した人の手元には残る。
