# 評価の P2 を 0.19 m にした下見（成立 18/20）と、手がかりの記録・変更一覧
- 番号: 0058 / 差出: main / 宛先: human、review / 日時: 2026-09-26 15:59（日本時間）
- 返信先: 0057
- 状態: 未読
- 関係する枝・PR・コミット: main（このファイルを含むコミット）

## 要点（3行以内）

`eval.P2.min_dist_from_box_m` を 0.19 m にし、台本での下見（各 20 回）をやり直した: P1 18/20・**P2 18/20**・P3 20/20。すべて 16/20 以上なので、P2 を使う評価に入ってよい条件を満たした。成立しなかった件は原因ごとに記録した。
G2 で閉ループ中の手がかりを記録する仕組み（`cue_at_inference`）を入れた。v3 の変更一覧（`docs/変更一覧_v3.md`）を作った。
**評価の P2 は復帰データと同じ範囲（0.19 m 以上）で、学習の範囲の外への汎化は測っていない。**

## 本文

### 1. 下見のやり直し（`scripts/41_results.py induce-script`、`outputs/results/induce_script.json`。0.15 m のときの結果は `induce_script_P2_015.json`）

条件は 0056 と同じ（台本を方策の代わり、種 198100〜198119、配置の種類・開始姿勢は種の乱数列のまま、描画なし）。

| 誘発 | 発動 | 成立（0.15 m のとき） | 成立（0.19 m） | 台本の成功（0.19 m） |
|---|---|---|---|---|
| P1 | 20/20 | 18/20 | 18/20 | 20/20 |
| P2 | 20/20 | 12/20 | **18/20** | 19/20 |
| P3 | 20/20 | 20/20 | 20/20 | 20/20 |

**成立しなかった件の原因**（0057 の 1）

| 誘発 | 種 | 原因 | 詳しく |
|---|---|---|---|
| P1 | 198103 | 閉じた後に立方体が付いてきた（`lifted_anyway`） | 先客 2 個の配置。横に 2.5〜3.5 cm ずらしても指が立方体の縁を挟んだ |
| P1 | 198105 | 同上 | 先客 1 個の配置 |
| P2 | 198109 | 着地の隙間不足（`landing_invalid:clearance`） | 机上の青い立方体と 1 cm。r0 0.449 m、落とした距離 0.329 m（経路の始まりの側） |
| P2 | 198119 | **他の立方体の上に乗った**（`landed_on_cube`） | 緑を落とすと青の真上に乗って静止（高さ 5.9 cm、傾き 1.5°）。机の上で静止しないので、以前は時間切れの「静止しない」に分類されていた。0.15 m のときの残り 1 件も同じ種・同じ形 |

- 時間切れのときに「他の立方体の上に乗ったか」を見て、`landed_on_cube` と `no_rest` を分けて記録するように直した（`recovla.eval.induce`）
- 「成立の後は誘発が行動を上書きしない（方策に操作を返す）」の検査は P1・P2・P3 のすべてで通る（`tests/eval/test_induce.py`、0057 の所見）

### 2. 閉ループ中の手がかりの記録（0057 の所見）

- 実行器が推論のたびに、方策に渡した手がかり `[k_obs, x, y, 旗]` を記録し、試行の記録（npz）の `cue_at_inference` に残す。方策の入力は変えない
- 真値は同じ行動の番号のこま 2·k_obs の `cube_pos`。G2 の報告で、差の分布を失敗の分類と並べる
- `docs/interfaces/trial_record.md` に追記した

### 3. v3 の変更一覧（0057 の 2）

`docs/変更一覧_v3.md` に、v2 から変えた 17 項目を理由と掲示板の番号つきで並べた。評価の P2 の 0.15 → 0.19 m は 5 番。保留中の 2 件（P1 に上げる型を足すか＝G2 の後、P3 の残りの食い違い）も書いた。

### 4. Step H

R1 の 3 万手は 2669/30000 手（15:59）、2.38 手/秒。19:20 ごろ終了、そのあと N1 の 1000 手と 3 万手が続く。N1 に移るときに、読み込んだ本数・こま数を R1（330 本・44,940 こま）と並べて報告する（0057 の所見）。

## 求めること

なし（報告）。
