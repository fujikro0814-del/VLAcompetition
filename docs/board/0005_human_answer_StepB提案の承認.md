# Step B 提案の承認

- 番号: 0005 / 差出: human / 宛先: main / 日時: 2026-09-25 17:20（日本時間。チャットでの指示を本線が書き写した。<記入> は原文のまま）
- 返信先: 0001
- 状態: 対応中
- 関係する枝・PR・コミット: なし

## 要点（3行以内）

Step B の提案書を承認する。4 点を直してから Step C に進む。
未決事項 1〜6・8 は承認。9 の支線は同時に 2 本まで、順序を指定。

## 本文

Step B の提案書を承認する。ただし次の4点を直してから Step C に進むこと。

1. 台本の段階（§8）に settle を足す。目標の速さが rest_speed 以上のあいだは静止して待つ。また、目標が箱の中にあるときは、静止前でも手を出さない（descend に入らない）。判定の順は done・retreat の直後に置く。B・C の注入の直後と、箱の上で放した直後に、この段階を通ることを phase_of の単体検査に入れる。
2. cloud/planner の持ち場から「30文と期待する出力」を外す。30文と期待出力は監督が書き、固定の試験データとして main に入れる。支線はプロンプトと検査のロジックだけを作る。
3. 「提出には GitHub の URL が要る」は未確定（提出の形は 10/8 に判明）。リポジトリは非公開のまま、公開はしない。コミットの作者はリポジトリだけの設定で、名前 <記入>、メールは GitHub の noreply アドレス <記入>。リポジトリ名は recovery_vla、アカウントは <記入>。
4. 箱の置き場所の条件を「着地した立方体どうしの隙間 2cm、壁との隙間 1cm 以上」に改め、box_slot_clearance_m の意味をこれに合わせる（放すのは壁の上端より上なので、指と壁の隙間は条件にしない）。

未決事項 1〜6・8 は承認。9 の支線は同時に2本まで。順序は、Step C の後に cloud/metrics と cloud/runner、Step D に cloud/expert、Step G の頃に cloud/planner と cloud/figures。各支線の指示書は、docs/interfaces/ の最初の文書が main に入った後に監督が掲示板に書く。

## 本線の対応（17:20 時点）

- 1・2・4 と支線の順序を `docs/B_提案書.md` に反映した（§5、§8、§14 R3、§15.2、§15.4）
  - settle の条件には「把持していない」を加えた（搬送中は立方体が手と一緒に動くので、加えないと carry が settle に取られる）。静止は「速さ < rest_speed が rest_hold_s（0.3 秒）続く」とし、done・retreat と同じ判定を使う
  - 4 について: 内寸 12 cm・2×2 の格子（中心 ±3 cm）では、向き 0° でちょうど条件の値になるだけで余裕がない。向きが 30° だと壁との隙間は約 3 mm。Step D で割合を示し、足りなければ相談する（§14 R3）
- 3 は 0002・0004 と食い違うので、0006 で確認している。答えが来るまで Step C には進まない
