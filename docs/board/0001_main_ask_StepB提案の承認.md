# Step B 提案の承認

- 番号: 0001 / 差出: main / 宛先: human、review / 日時: 2026-09-25 16:15（日本時間）
- 返信先: なし
- 状態: 未読
- 関係する枝・PR・コミット: なし（リポジトリは Step C で作る。このファイルもそのときに初めてコミットする）

## 要点（3行以内）

Step B の提案書 `docs/B_提案書.md` を出した（コードは書いていない）。
承認をお願いしたい点は 9 つ（§14 の末尾）。支線の候補 5 本は §15 にある。
承認が来るまで Step C に進まない。

## 本文

- 提案書: `docs/B_提案書.md`（§1〜§15）。前提は `docs/A_調査報告.md` と、Step A への判断（計画書 v2・手順書 v2）
- Step B のために追加で確かめた事実（C:\VLA を読むだけ）は、提案書の冒頭に出所つきで書いた。主なもの: MuJoCo 3.2.3 に `mj_getState`・`mj_geomDistance` がある（`mujoco.h:411, 499`）。LeRobot 0.6.1 の統計量は `meta/stats.json` から読まれる（`datasets/io_utils.py:161-174`）。学習率は方策の設定から作られる（`configs/train.py:272-276`）
- 主な提案:
  - 環境: 1 つの環境（Python 3.12.14、lerobot 0.6.1、mujoco 3.2.3）で生成・学習・評価を回す。uv と Python 本体は `<ROOT>` の下。学生用の 95 パッケージを版の固定一覧どおりに入れ、追加は scipy・matplotlib・anthropic だけ（§3）
  - 取り込み: 流用元のコミット dbb2c3b から `git archive` で取り出し、`docs/provenance.csv` に SHA-256 を記録する（§2）
  - 台本: 毎周期、真値から段階を判定する。専用の立て直しの処理は作らない（§8）
  - 乱数: 名前つきの独立した乱数列。誘発の条件は試行の開始時に全部決める。k 回目の推論の雑音は (種, k) から作る（§9）
  - 2 周目: 学習に渡すデータセットの `meta/stats.json` を R1 のものに差し替えて統計量を固定し、学習後にビット単位で照合する。学習率はピーク 3e-5 → 1 万手で 2.5e-6（§10）
  - GitHub: gh CLI で非公開のリポジトリを作り push する。認証は人の操作（§3.2 の 10）
- 危険: 箱が 3 個には狭い（内寸 12 cm に一辺 4 cm を 3 個）。まず 2×2 の置き場所で進める（§14 の R3）

## 求めること

`docs/B_提案書.md` §14 末尾の 9 点についての判断。

1. ディレクトリ構成とパッケージ名 `recovla`
2. `git archive` による取り込みと、uv・MinGit・gh の GitHub からの取得
3. `ca_bundle_proxy.pem`（公開の証明書）を `.local\` に複写すること
4. 2 周目の学習率（ピーク 3e-5、立ち上がり 200 手、1 万手で 2.5e-6）
5. 種の帯の割り当て（§9）
6. LLM は `claude-haiku-4-5`
7. Git の作者名・メールアドレス、GitHub のリポジトリ名とアカウント
8. 箱の狭さ（R3）の進め方
9. 支線の分け方（§15: `cloud/expert`、`cloud/metrics`、`cloud/runner`、`cloud/planner`、`cloud/figures`。境目と開始の条件）

後戻りのしやすさ: どれも Step C の前なので、変更の費用は提案書の書き直しだけ。ただし 5（種の帯）と 9（境目）は、データを作り始めた後や支線が動き出した後に変えると手戻りが大きい。
