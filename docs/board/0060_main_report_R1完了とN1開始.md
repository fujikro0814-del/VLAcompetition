# R1 の 3 万手が完了、N1 の 3 万手を開始
- 番号: 0060 / 差出: main / 宛先: human、review / 日時: 2026-09-26 19:20（日本時間）
- 返信先: 0055、0057、0059
- 状態: 未読
- 関係する枝・PR・コミット: main（学習は c7886d9 のコード。このファイルと `docs/media/2026-09-26_StepH/` を含むコミット）

## 要点（3行以内）

R1 の 3 万手が 19:12 に完了（12,724 s、終了コード 0、GPU の最大 7.69 GiB、保存点 5000〜30000 の 6 つ）。損失は 1.84 → 0.020 で、最後の 2 割はほぼ平ら。
N1 の 1000 手の試しは合格（330 本・46,225 こまを読み込み。R1 と同じ 330 本、こま数の差 2.8% は Step F の見込みどおり）。19:19 に N1 の 3 万手を開始、22:50 ごろ終了の見込み。
学習の設定に外部への送信がないこと（`push_to_hub=false`・`wandb.enable=false`）を記録で確かめた。

## 本文

### R1（`outputs/h/train_R1.json`、`outputs/train/train_R1_20260926-153959_20260926-153959`）

| 項目 | 値 |
|---|---|
| データ | `outputs/datasets/R1cue_20260926-140506`（手がかり 17 次元、旗は記録のみ） |
| 読み込んだ本数・こま数 | 330 本・44,940 こま（データセットの指紋 54367448…） |
| 手数・保存 | 30,000 手、5000 手ごと（005000・010000・015000・020000・025000・030000） |
| 時間・速さ | 12,724 s（3.53 時間）、平均 2.36 手/秒 |
| 損失 | 最初 1.84 → 最後 0.020。最後の 1 割の平均 0.0202、その前の 1 割 0.0202 |
| GPU の最大 | 7.69 GiB（上限 14 GiB を超えない） |
| 設定 | 種 1000、バッチ 32、行動エキスパートのみ、学習率は既定（LoRA・重み・手がかりのずらしはなし） |
| 外部への送信 | `--policy.push_to_hub=false`、`--wandb.enable=false`（`train_run.json` の command で確認） |
| コード | c7886d9（未コミットの変更なし）。学習の記録（データセット・設定・コミット・conversion.json・正規化の統計量）は学習の出力先にそろっている |

損失の図: `docs/media/2026-09-26_StepH/R1_loss.png`。

### N1 の開始（0057 の所見: 本数・こま数を R1 と並べる）

| | R1 | N1 |
|---|---|---|
| データセット | `R1cue_20260926-140506` | `N1cue_20260926-140506` |
| 読み込んだ本数 | 330 | **330** |
| 読み込んだこま数 | 44,940 | 46,225（差 1,285 こま＝2.8%。復帰 90 本と、同じ配置・目標の通常 90 本の長さの差。Step F の完了条件 5 の値と同じ） |
| 通常の部分 | 240 本（同じエピソード） | 240 本（同じエピソード） |
| 種・設定 | 1000、同じ | 1000、同じ |
| 1000 手の試し | 合格（445.8 s、損失 0.788 → 0.100） | 合格（445.4 s、損失 0.754 → 0.089、GPU 7.69 GiB） |

N1 の 3 万手は 19:19 に開始（2.42 手/秒）。22:50 ごろ終了の見込み。

### この後（0055 の順番）

N1 の完了の後: Step G の残りの検査（方策を GPU で動かすもの: 完了条件 1・2・6、条件 4 を方策で）→ 遅れ d の決定（`41_results.py dcal`）→ RTC の設定の決定（R1 で、2×2、P2 は 0.19 m）→ 保存点の選択（R1・N1、2 万と 3 万）→ G2。N1 の学習中は、学習と取り合わない Step G の作業だけをする。

## 求めること

なし（報告）。
