# 0036・0039 への判断（決裁の回答を本線が書き写した）
- 番号: 0040 / 差出: human（本線が書き写し） / 宛先: main、review / 日時: 2026-09-26 09:55（日本時間）
- 返信先: 0036、0039
- 状態: 対応中
- 関係する枝・PR・コミット: main

## 要点（3行以内）

0039（LoRA）は提案どおり承認（8〜15 層の q_proj・v_proj、rank 16、最初の 2 秒を 5 倍、配置 100、8000 手、peft はネットワークから入れて版を固定）。追加の条件 5 つ。
0036（B の距離の下限）は 0.19 m に決める。コードは変えない。A の決定は確認した。

## 本文

チャット（2026-09-26）での決裁の回答:

### 0039（LoRA）: 提案どおり承認

- 対象: SmolVLM2 の 8〜15 層の `q_proj`・`v_proj`、rank 16。行動エキスパートと射影層は全部学習。学習率は変えない
- 0038 の 2 つ（最初の 2 秒を 5 倍の重み、K1 の配置を 100＝種 10000〜10099）と 8000 手も承認
- peft はネットワークから入れ、版を固定して記録する
- 条件（追加）:
  1. 並べ方の包みの単体検査で、実際に引かれた「最初の 2 秒のこま」の割合が期待値に合うことを確かめる
  2. 50 手の試しの後、保存したモデルを推論の経路で読み込み、同じ入力・同じ雑音で学習時の出力と一致すること（LoRA の差分が推論で効いていること）を確かめてから 8000 手に進む
  3. 報告には E6・閉ループに加えて D1（cos の分布・取り違え表）と D2 を測り直して入れる
  4. E6 は 190000〜190032、閉ループは 191000〜191029 のまま（前回の K1 と対で比べる）
  5. 学習中のシミュレーションは 2 本まで（0034 の 1）
- 不合格なら、次の分岐は決めずに報告して止まる

### 0036（B の距離の下限）: 0.19 m に決める。コードは変えない

- 理由: 0.19 m で着地が不自然 14.2%（≤ 20%）、壁・箱の縁 0、除外 0。「机上の他の立方体」の件（16〜19 件）は下限では動かないが、20% の枠に収まり、作り直しで捨てるだけで済む
- 落とす時点でほかの立方体からの距離を条件に足す案は、今は入れない（不自然の割合が 20% を超えたら改めて諮る）
- 評価の P2 を 0.19 m に揃えるかは、Step G で改めて諮る
- 設定に書き込み、Step F の完了条件の試験（`30_f.py check-gen`）は、描画が学習と重ならない時間に回す
- A の決定（`raise_close_m` [0.025, 0.05]、`raise_ratio` 0.5）は確認した

## 本線の段取り

B を設定に書き込む → peft を入れる → 並べ方の包み・LoRA の設定を起動器に足す（単体検査）→ 配置 100 のデータを作って変換 → Step F の完了条件の試験（描画。学習の前に済ませる）→ 50 手の試しと推論の経路の一致の確認 → 8000 手 → E6・閉ループ・D1・D2 → 報告して止まる。
