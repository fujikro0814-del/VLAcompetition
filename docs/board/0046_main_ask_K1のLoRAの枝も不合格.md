# K1 の LoRA の枝も不合格（次の分岐は決めずに止まる）
- 番号: 0046 / 差出: main / 宛先: human、review / 日時: 2026-09-26 11:33（日本時間）
- 返信先: 0040、0042
- 状態: 未読
- 関係する枝・PR・コミット: main（このファイル、`docs/E_報告.md` の追記、`docs/media/` を含むコミット）

## 要点（3行以内）

LoRA（8〜15 層の q・v、rank 16）＋最初の 2 秒を 5 倍＋配置 100 で 8000 手をやり直したが、**不合格**。E6 の正答率 31.3%（前回と同じ）、閉ループの成功 1/30、持ち上げ 5/30（うち 4 本は違う色）、接触率 63.3%。
D1 の cos の中央値は −0.09 → 0.34 とわずかに正に寄ったが、閉ループは改善しない。手順書の判定表の「再試行でも不合格 → 止めて報告し、色の差を強める／間隔を広げる／2 色に減らす、の順で相談」に当たる。
0040 のとおり、次の分岐は決めずに止まる。R1・N1 の生成（0044）は K1 と独立なので、この報告の後に順に進める。

## 本文

詳細は `docs/E_報告.md` の「追記（2026-09-26 11:33）」。数字の出所は `outputs/k1/*_lora.json`・`diag_lora.json`・`lora_check_final.json`（Git 管理外）。

### 前回と対で（同じ種: E6 は 190000〜190032、閉ループは 191000〜191029。0040 の条件 4）

| | 最初の K1 | LoRA の枝 |
|---|---|---|
| E6 正答率（多数決／5 回ごと） | 31.3%／33.5% | 31.3%／36.2% |
| 中間落ち率 | 78.4% | 71.1% |
| 閉ループ: 成功／持ち上げ／違う色／接触 | 2／3／1／14（30 回中） | 1／5／4／19 |
| 失敗の届いた段階（接近・把持・搬送・設置） | 28・0・0・0 | 25・4・0・0 |
| D1 cos（10・50・90 百分位）、平均 | −0.94・−0.09・0.96、0.02 | −0.94・0.34・0.97、0.12 |
| D1 予測のずれ ÷ 正解のずれ（中央値） | 0.37 | 0.27 |
| D2 正しい指示の損失が最小（学習データ） | 56.9% | 50.6% |

D1 の取り違え表（LoRA の枝、行 = 指示、列 = 最寄り、各 5 回）:

| 指示 \ 最寄り | red | green | blue |
|---|---|---|---|
| red | **71** | 42 | 52 |
| green | 71 | 56 | 38 |
| blue | 59 | 54 | 52 |

### 決裁 0042 の関心（重みで後半のこまが減った影響）

- 失敗はほぼ全部が接近の段階で止まっている（25/29）。掴む段階まで進んだ 4 本はすべて違う色を掴んだ。搬送・設置まで進んだ試行が両方のモデルでないので、この 30 回では後半の段階の落ち込みは判断できない

### 事前の確認（0040 の条件 1・2）

- 引いた最初の 20 こまの割合: 8000 手の全体で 0.456（期待値 0.457、抽出 256,776 回）
- 最終の保存点を推論の経路で読んで、学習時と同じ TF32 の設定で学習時の出力とビット一致。アダプタを切った出力とは 2.97 違う

### 映像（決裁の指示で GitHub に入れた）

- `docs/media/2026-09-26_K1/`: 両モデルの閉ループの先頭 6 回（種 191000〜191005）。一覧は同じフォルダの `README.md`
- `docs/media/2026-09-26_StepF_check/`: Step F の完了条件 4 の 15 本（0045 の 1）

### 決裁の指示の書き写し（チャット、2026-09-26 11:30 ごろ）

- 「今後、動画など確認用のものは GitHub に入れられたら入れる」
- 本線の運用: `docs/media/<日付>_<題>/` に置き、フォルダごとに `README.md` で中身を示す。入れるのは本課題のシミュレーションの映像・画像だけ（公開リポジトリなので、C:\VLA の記録・卒研の資料・個人の情報は入れない）。1 本は数 MB で、1 回の報告で数十 MB まで。大きくなる場合は先に諮る

## 求めること

K1 の次の手（手順書 §6 の判定表の最後の行。本線からは決めない）。参考までに、手順書の相談の順は (1) 色の差を強める、(2) 立方体の間隔を広げる、(3) 最後の手段として 2 色に減らす。

答えが来るまで、K1 の分岐の実装と学習はしない。0044 の R1・N1 の生成（`gen-data --smoke` → 本番）と、その後の目視用の資料（0045 の 2）は、K1 の判断と独立なので順に進め、完了条件 5 は別の番号で報告する。
