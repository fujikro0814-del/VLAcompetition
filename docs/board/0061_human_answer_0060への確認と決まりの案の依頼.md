# 0060 への確認と、学習の後の決まりの案の依頼（決裁の回答を本線が書き写した）
- 番号: 0061 / 差出: human（本線が書き写し） / 宛先: main、review / 日時: 2026-09-26 20:10（日本時間）
- 返信先: 0060
- 状態: 対応中（案は 0062）
- 関係する枝・PR・コミット: main

## 要点（3行以内）

R1 の完了と N1 の開始を確認、問題なし。保存点は損失ではなく、選択用の種での成功率で選ぶ（計画書のとおり）。N1 の報告にはこま数の差とその理由をそのまま書く。
N1 の学習中に、学習の後の手順で使う決まり（遅れ d・RTC の設定・保存点の選択・種の割り当て）を、結果を見る前に文書に固めて諮る。

## 本文

チャット（2026-09-26）での決裁の回答:

回答（0060）: R1 の完了と N1 の開始を確認した。問題なし。N1 の 3 万手はそのまま続けてよい。

R1 について:
- 損失の下がり方、GPU の使用量、外部への送信の停止、種・データの指紋・コミットの確認、すべて了承
- 損失は最後の 2 割で平らだが、保存点は損失ではなく、選択用の種での成功率で選ぶ（計画書のとおり）

N1 について:
- 330 本で R1 と揃っていることを確認した。こま数の 2.8% の差は Step F の見込みどおりなので了承。報告には差とその理由をそのまま書く

N1 の学習中にしておくこと（CPU だけ・学習と取り合わないもの）: 学習の後の手順で使う決まりを、結果を見る前に文書に固めて掲示板に出す。案を作って諮ってほしい。
1. 遅れ d の測定: 学習も他のシミュレーションも止めた状態で、R1 の 30000 手の保存点で測る。回数と、`delay_rule`（p95 / 0.1 s を切り上げ、d > 5 なら止める）の適用を明記する
2. RTC の設定の決定: R1 の 30000 手の保存点で行う（保存点の選択より前に来るため）。2×2 の各組を選択用の種の P2 誘発 30 回ずつ。「成功率が大きく落ちない」の数値の線（例: 最良の組から何回以内）と、継ぎ目の跳び・反応時間のどちらを先に見るかの順位、同点のときの決め方を先に決める
3. 保存点の選択: 候補にする保存点（例: 20000・25000・30000 手）、試行の回数と種、実行の設定（rtc・s=10・上の d と RTC の設定）、同点のときの決め方（例: 後の保存点を採る）。R1 と N1 に同じ決まりを当てる（E3 で両者を公平に比べるため）
4. 上の 1〜3 で使う種は、すべて選択用（190000〜）から、どの判断にどの帯を使うかを重ならないように割り当てる

順番は 0055 のとおり（Step G の残りの検査 → d → RTC の設定 → 保存点の選択 → G2）。次の報告は、上の決まりの案（先に）と、N1 の完了の時点でよい。
