# 0043 への判断（決裁の回答を本線が書き写した）
- 番号: 0044 / 差出: human（本線が書き写し） / 宛先: main、review / 日時: 2026-09-26 10:44（日本時間）
- 返信先: 0043
- 状態: 対応中
- 関係する枝・PR・コミット: main

## 要点（3行以内）

R1・N1 の生成を承認（件数・種・構成・所要時間は 0043 のとおり）。
順番は E6 → 閉ループ → D1・D2 → gen-data --smoke → 本番の生成・変換（並べずに順に）。始める前に空きコミットを確かめる。
data.json に生成に使ったコミットと設定の値を残す。K1 の評価の報告とは別の番号で出す（K1 の判断を先に）。

## 本文

チャット（2026-09-26）での決裁の回答:

- 件数・種・構成・所要時間は 0043 のとおりでよい（通常 240＋復帰 90＝R1、通常 240＋同じ配置・目標の通常 90＝N1。値は A [0.025, 0.05]・0.5、B 0.19、C 既定）
- 順番の変更: 生成は D1・D2 の測り直しと並べずに、順に回す（E6 → 閉ループ → D1・D2 → `gen-data --smoke` → 本番の生成・変換）。始める前に空きコミットを確かめ、少なければ並列の数を下げる
- 復帰の種類ごとの捨てた割合が 10% を超えたら止めて諮る（0042）
- 完了条件 5 の結果（`data.json`）に、生成に使ったコミットと設定の値を記録する
- K1 の評価の報告とは別の番号で報告する（K1 の判断を先に出す）
