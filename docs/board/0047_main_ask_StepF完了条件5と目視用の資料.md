# Step F の完了条件 5（R1・N1 のデータ）は合格。目視用の資料を出して止まる
- 番号: 0047 / 差出: main / 宛先: human、review / 日時: 2026-09-26 12:06（日本時間）
- 返信先: 0044、0045
- 状態: 未読
- 関係する枝・PR・コミット: main（このファイルと `docs/media/2026-09-26_StepF_review/` を含むコミット）。生成は 6b147b1（未コミットの変更なし）

## 要点（3行以内）

R1（330 本・44,940 こま）と N1（330 本・46,225 こま）を作った。配置と目標の集合が一致し、構成は計画どおり、変換の検証も両方合格、フレーム数の差 2.8%。**完了条件 5 は合格**で、Step F の完了条件 1〜5 がそろった。
捨てた割合は、復帰 A 0/40・B 1/31（3.2%）・C 0/20、通常 0/240（止める線の 10% に届かない）。
目視用の資料（種類ごとに 10 本の映像、復帰 90 本の最初のこまの一覧）を `docs/media/2026-09-26_StepF_review/` に入れた。**決裁の承認まで Step H に進まない。**

## 本文

### 完了条件 5（`scripts/30_f.py gen-data`、`outputs/f/data.json`、`tests/test_f_recovery.py` 7 件合格）

- 手順: 0044 のとおり E6 → 閉ループ → D1・D2 の後、空きコミット 36.6 GB を確かめて、`gen-data --smoke`（変換・検証まで合格）→ 本番を 8 並列で回した（生成 169 秒、変換と検証は R1 703 秒・N1 716 秒、11:37〜12:03）
- 生成のコミット 6b147b1 と、設定の値（`inject`・`expert`・`scene`・`sim`）を `data.json` に記録した（0044）

| | R1 | N1 |
|---|---|---|
| エピソード | 330（通常 240＋復帰 90） | 330（通常 240＋同じ配置・目標の通常 90） |
| こま（10 fps） | 44,940 | 46,225 |
| 変換・検証 | 合格 | 合格 |
| マニフェスト | `outputs/manifests/R1_20260926-113711.json` | `outputs/manifests/N1_20260926-113711.json` |
| データセット | `outputs/datasets/R1_20260926-113711` | `outputs/datasets/N1_20260926-113711` |

- フレーム数の差: 2.8%（見積もり 3.7%、報告の境 10%）
- 配置と目標の集合: R1 の復帰 90 と N1 の相手の通常 90 で一致（マニフェストの名前から数え直した）。通常 240 は同じエピソード

**構成表**

| 群 | 本数 | 色（red・green・blue） | 配置の種類（空の箱・先客 1・先客 2） | 開始姿勢（home・retreat） |
|---|---|---|---|---|
| 通常（共通） | 240 | 80・80・80 | 180・30・30 | 162・78 |
| 復帰（R1）／相手の通常（N1） | 90 | 32・31・27 | 67・12・11 | 57・33 |
| うち A | 40 | 17・12・11 | 30・5・5 | 26・14 |
| うち B | 30 | 9・11・10 | 22・4・4 | 16・14 |
| うち C | 20 | 6・8・6 | 15・3・2 | 15・5 |

- 復帰の割合（R1 のこまのうち復帰の区間）: 90 本 × 平均約 12 s。通常は平均約 14 s

**捨てた割合（決裁 0042 の別の指標）**

| 群 | 捨てた／見た | 区間 |
|---|---|---|
| 復帰 A | 0/40 | [0.000, 0.088] |
| 復帰 B | 1/31（3.2%） | [0.006, 0.162] |
| 復帰 C | 0/20 | [0.000, 0.161] |
| 通常 | 0/240 | [0.000, 0.016] |

### 目視用の資料（0045 の 2）: `docs/media/2026-09-26_StepF_review/`（43.9 MB、決裁の指示で GitHub に入れた）

- `first_frames_A.png`・`_B.png`・`_C.png`: 復帰 90 本すべての保存した最初のこま（俯瞰・手首）。見出しに種類・種・色・向きの角（A は型も）
- 種類ごとに 10 本の映像: 同じ種で最初から回し直し、注入の 1 秒前から立て直しの完了まで。左上に「注入の前／注入（学習に入らない）／保存した部分（学習に入る）」を表示。30 本すべて、記録を始めた物理ステップが保存したものと一致し、成功した
- 一覧は同じフォルダの `README.md`

### 資料を作って気づいたこと（0045 の 4 に関わる）

1. **B・C の落下後の向きがほぼ 0°**: 指に対する向きの角（90° の対称性を除く）が、C は 20 本すべて 0.0°、B は 30 本中 28 本が ±2° 以内（最大 19.5°）。掴んで指と向きがそろった立方体を真下へ落とす・置くので、回らずに着地する。A は掴めていないので、配置のときの向きのまま（中央値 20°、最大 38°）。方策の自然な失敗で落ちた立方体が回っていれば、復帰データの範囲の外になる。G2 の分布の比較（0045 の 4）で、向きの角をはっきり見る
2. **A の型が横ずらしに寄った**: 40 本のうち横ずらし 25・上げる型 15（`raise_ratio` は 0.5）。作り直しのたびに型を引き直すので、採られた試みの型が偏ったと見ている（捨てた A は 0/40）。型の割合が 0.5 のとき、上げる型が 15 本以下になる確率は片側で約 8%（両側で約 15%）

## 求めること

1. 目視用の資料の承認（承認まで Step H の学習に進まない＝0045 の 2）
2. 上の 1・2 を、学習の前に手当てするか。向きをばらすのに手首を回すことはスコープ外（手順書 §0.3「手首のヨー回転」）なので、手当てするなら注入の側（落とし方など）で考える必要がある。本線からは決めない

K1 の次の手（0046）も答え待ち。
