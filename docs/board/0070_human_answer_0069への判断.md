# 0069 への判断（R2 の計画の承認、推論の高速化の見積もり、分類の定義の承認、作品の方針、E4 の見直し）
- 番号: 0070 / 差出: human（決裁）/ 宛先: main / 日時: 2026-09-27 01:13（日本時間。本線が書き写した時刻）
- 返信先: 0069
- 状態: 既読（本線）
- 関係する枝・PR・コミット: main（このファイルを含むコミット）

## 要点（3行以内）

切り分け（主因は遅れそのもの）と 0067 の訂正を了承、依頼外の診断は事後承認（以後は回す前に掲示板に一行）。R2（乙）の計画を承認、実装は今から、生成は 2 の報告と回答の後。
2 の (1) N1 の P1・P3 を sync で、(2) 遅れ d を下げる手の見積もり（採用はしない）。保存点の候補 5000・10000 を承認。丙は行わない。分類の定義は 3 点を加えて承認（(b) 成功した試行での誤発火の検査が先）。P1 の上げる型は足さないで確定。
作品の方針: 本線は非同期（評価の設定）で、R2 の結果によらずその数字を報告する。E4 は「遅れと立ち直りの取引」を示す実験として、E の評価の前に案を出す。

## 本文（決裁の回答の原文）

決裁の回答（0069）

■ 報告事項の受け取り
- 評価の設定そのもの（とくに遅れ d=4）が立ち直りを止めている、という切り分けを了承する。範囲 10 でも P3 は 1/29、naive でも P3 5/29・手を止める P2 1/17 で、主な原因は RTC の範囲より遅れそのものと読む。変更一覧 v3 の「結果として書くこと」に加えた知見の書き方を了承する
- 0067 の訂正（戻った時点でグリッパは開、距離 7.6 cm）を了承する
- 依頼外の追加の診断は、同じ種で種を増やしていないこと、報告で分けて書くことを確認し、事後承認とする。以後、依頼外のものを回すときは、回す前に掲示板に一行で知らせる

1. R2 の計画（乙）: 案のとおり承認する
  - 評価と同じ設定（rtc・s=10・d=4・範囲 40・指数）で走らせる。評価の設定は変えない
  - 配分（乙 120: P1・P2・P3 各 40／甲 60）、引き継ぎの時点の半々、種の割り当て、生成の後の検査 (a)〜(c)、目視の承認まで学習しないこと、すべて案のとおり
  - 実装は今から進めてよい。生成は下の 2 の結果の報告と、その回答の後にする

2. R2 の実装と並行して、GPU が空いている間に次を回す（生成より前に結果を出す）
  (1) N1 で P1・P3 の誘発を sync で各 30 回（195100〜195129、R1 の sync の結果と対にする）。A・C の復帰データが、遅れのない実行で上積みを出しているかを見る
  (2) 推論を速めて遅れ d を下げる手の見積もり（採用はしない）。候補: 流れの積分の刻み数を減らす、推論の経路のコンパイル、そのほか出力を変えない高速化。それぞれについて、推論時間の p95 と、決まり（p95 / 0.1 s の切り上げ）で出る d、今の推論と同じ入力で比べた行動の塊の差（固定した入力 100 組で、平均と最大）を示す。出力を変える手（刻み数の削減など）と変えない手を分けて書く
  報告の後に、d を下げる手を R2 の生成より前に入れるかを判断する（入れる場合、R2 はその設定で集めるため、生成の前でなければならない）

3. R2・R1+ の保存点の候補を 5000 手と 10000 手とし、同じ種（193000〜193029）・同じ設定・同じ規則（多い方、同数なら 10000 手）で選ぶ案を承認する

4. 丙は行わない（案のとおり）。見積もりの「開始の瞬間を合わせるだけでは足りない（遅れの間の動きまで覆う必要がある）」は、行わない理由として報告に書く

5. 自然な失敗の分類の定義: 次の 3 点を加えて承認する
  (a) 落下 (ii) で、机の上以外（箱の縁、他の立方体の上など）で静止したものを「その他の落下」として分ける（今の定義ではどこにも入らない）
  (b) 計算の前に、G2 で成功した試行（R1 96 本・N1 96 本）に (i)〜(iv) を当て、成功の前に誤って発火した件数を示す。とくに (iii) 誤った色への接近と (iv) 停滞（把持の前後の静止）が、成功した試行で発火しないことを確かめる。発火があれば、閾値を変える前に諮る
  (c) R2 の甲の失敗の検出にもこの定義を使う（案のとおり）。(b) の検査を通ってから使う

6. P1 に閉じる高さを上げる型は足さない（案のとおり）。これで確定とし、以後この件は諮らなくてよい

次の報告: 2 の (1)(2) の結果と、5 の (b) の検査の結果。R2 の実装が済んでいれば、その検査の結果も併せて。そこで止まる。0069 への回答の追記（作品としての方針。R2 の結果より前に固定する）

7. 本線の実行は非同期（評価の設定: rtc・s=10・d=4・範囲 40・指数）とし、R2 の結果がどうであっても、その設定での数字をそのまま報告する。R2 で P2 が戻らなかった場合も、本線の設定は変えない
  - この方針は R2 の結果を見る前に決めたものとして、変更一覧 v3 と最終報告に日付つきで書く
  - 遅れを下げる手（2 の (2)）を採った場合は、その手で決まった d の非同期が本線になる。採否は見積もりの報告の後に決める

8. E4 は「遅れと立ち直りの取引」を示す実験として設計を見直す。E の評価に入る前に案を出して止まる。案に入れるもの:
  - 条件: sync、naive d=4、rtc d=4（本線の設定）。遅れを下げる手を採った場合は、その d の条件も加える
  - 測るもの: これまでの継ぎ目の跳び・躍度・反応時間に加えて、P1・P2・P3 の誘発からの復帰率（成立した試行が分母）と、自然な試行の成功率
  - 同じ種の組を全条件で使う。種は選択用の帯から割り当て、台帳に記録する（最終評価の Step J では最終評価用の帯で同じ設計を回す）
  - 図は「滑らかさ（跳び）」と「立ち直り（復帰率）」を条件ごとに並べ、取引が一目で分かる形にする

## 本線の対応

- 2 の (1): 01:13 に回し始めた（N1、sync・s=10、195100〜195129、P1・P3 各 30。条件 `G2diag/N1_P1_sync`・`N1_P3_sync`）
- 2 の (2): 固定した入力 100 組は、学習用の帯ではなく Step G の検査の帯の未使用の範囲（198700〜）で R1 を評価の設定で走らせて取る（`docs/種の台帳.md` に書く）
- 7: `docs/変更一覧_v3.md` に日付つきで書いた。6: 変更一覧の保留から外した（確定）
- 依頼外のものを回すときは、回す前に掲示板に一行で知らせる
